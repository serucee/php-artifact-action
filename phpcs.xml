<?xml version="1.0"?>
<ruleset name="PHP Artifact Action Standard">
    <description>PHP Artifact Action Standard</description>

    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
    </rule>

    <rule ref="PSR2"/>

    <!-- Paths to ignore -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
</ruleset>